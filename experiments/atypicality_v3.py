"""Этап 4b: лестница нетипичности на пуле v3 = v2 + 8 геологических факторных слоёв.

ЗАЧЕМ ЭТОТ ПРОГОН
-----------------
В этапе 4 собственный алгоритм проиграл критериальному методу при НЕРАВНЫХ
входных данных. Критериальный прогноз ГИС Интегро вычислен по восьми слоям
геологической карты (разломы СЗ и СВ и их плотность, дайки и их плотность, коры
выветривания, литолого-фациальные зоны, палеодолины), а наш пул эти слои не
содержал: они перечислены в ``config.CRIT_EXCLUDE_FACTOR_FEATURES`` и режутся
в ``cell_mask.feature_columns``.

Запрет был введён в ПРЕДЫДУЩЕЙ фазе и там был обязателен: модель обучалась на
псевдометках критериального анализа, и, получив его же входы, тривиально
восстановила бы его формулу. В нынешней постановке обучения на метках нет ни на
одном шаге — детектор нетипичности не видит ``prognoz`` вообще, — поэтому
циркулярность невозможна, а запрет превратился в фору критериальному методу.

Пул v3 эту фору снимает: оба метода получают одну и ту же геологию, и разница в
результате относится к алгоритму, а не к доступу к данным.

Проверено перед прогоном: 8 слоёв заполнены на 100%, не дублируют имеющиеся 44
признака (max |r| = 0.68 у dist_struct с dem_elev, у остальных 0.19-0.49),
обусловленность пула 2.06e3 -> 2.30e3, то есть вырождения не вносят. Отдельно:
наши автоматические линеаменты коррелируют с картированными разломами лишь на
0.29 — то есть группа lin геологическую тектонику НЕ замещала.

КРИТЕРИЙ ПРЕВОСХОДСТВА — ЗАФИКСИРОВАН ДО ПРОГОНА
------------------------------------------------
Первичная метрика прежняя (``config.VER_PRIMARY``): lift@10% на строго
несмещённых точках заверки, пул — валидные ячейки.

Собственный алгоритм считается превзошедшим критериальный, если выполнены ОБА
условия одновременно:

1. нижняя граница 90% бутстрэп-интервала разности (собственный минус
   критериальный) при ресэмплинге ПО ПРОСТРАНСТВЕННЫМ КЛАСТЕРАМ точек строго
   больше нуля — то есть превосходство не держится на одной-двух точках;
2. сдвиговый (тороидальный) null самого собственного скора даёт p ниже порога
   Бонферрони 0.05/18 = 0.0028, где 18 = 9 ступеней (8 детекторов + ансамбль)
   x 2 режима (глобальный и доменный) — иначе «победитель» выбран той же
   метрикой, по которой оценивается.

Честная оценка достижимости, тоже до прогона: при 19 точках и площади 10% одна
точка стоит 0.53 lift, поэтому условие 1 требует перевеса примерно в 5-6 точек
из 19. Это высокая планка, и она поставлена намеренно: заявлять превосходство
слабее — значит выдавать шум за результат. Если планка не взята, корректная
формулировка результата — «методы неразличимы на имеющемся объёме заверки»,
а не «критериальный лучше».

Диагностика, НЕ входящая в критерий (нужна для карты расхождений этапа 7):
ранговая корреляция карт собственного и критериального скоров.

Выход: ``outputs/metrics/atypicality_v3{,_abl,_null,_boot,_contrib}.csv``,
``outputs/atypicality_v3_map.png``, ``outputs/atypicality_v3_ablation.png``,
``outputs/metrics/atypicality_v3_verdict.csv``.

Запуск из корня: ``python -m experiments.atypicality_v3``.
"""
import sys
from pathlib import Path

ROOT = Path(__file__).resolve().parents[1]
sys.path.insert(0, str(ROOT))

from experiments import atypicality_v2  # noqa: E402

#: Порог Бонферрони: 9 ступеней x 2 режима нормировки.
N_CONFIGS = 18
ALPHA = 0.05 / N_CONFIGS

if __name__ == "__main__":
    atypicality_v2.main(include_factors=True, stem_name="atypicality_v3",
                        pool_name="v3")
