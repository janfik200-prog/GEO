# Окружение
.venv/
venv/

# Python
__pycache__/
*.py[cod]
.ipynb_checkpoints/
.pytest_cache/

# Результаты и данные
*.png
outputs/
metrics/
ml_png_result/
prog_zip/
data/

# Постоянный кэш скачанных данных (геофизика/NURE/MRDS/CMMI) — НЕ публиковать (сотни МБ)
datacache/

# Черновики разовых замеров в корне: массивы, сброшенные из интерактивной сессии.
# Результаты прогонов кладутся в outputs/, а не в корень, поэтому здесь такие
# файлы — всегда мусор (в августе 2026 их набралось 35 МБ)
/*.npy
/*.npz

# ОС
desktop.ini
.DS_Store
